You are an expert AI systems engineer.

I am building an AI educational video generation pipeline using:

- Gemini API
- NVIDIA NIM API
- Piper TTS
- WhisperX
- Manim
- FFmpeg

The project already exists but dependencies and architecture are broken.

Your task is to FULLY REFACTOR and SETUP the entire project into a working production-style architecture.

IMPORTANT REQUIREMENTS:

==================================================
GOAL
==================================================

User enters:

"Explain Newton's First Law"

Pipeline should:

1. Generate educational explanation
2. Generate Scene JSON
3. Generate narration script
4. Generate visual skeleton
5. Generate Manim code
6. Generate narration audio
7. Extract audio durations
8. Synchronize scenes
9. Inject timing into Manim
10. Render video
11. Merge audio + video

==================================================
ARCHITECTURE
==================================================

Implement this exact architecture:

Student Query
      ↓
Gemini
      ↓
Scene JSON (Master Plan)
      ↓
 ┌────────────────────┬────────────────────┐
 ↓                    ↓                    ↓
Narration Script     Visual Skeleton      Metadata
 ↓                    ↓
Piper TTS           Timing-Free Manim Plan
 ↓                    ↓
Audio Files         Scene Objects
 ↓
WhisperX Forced Alignment
 ↓
Word-Level Timestamps
 ↓
Scene Synchronization Engine
 ↓
Timeline JSON
 ↓
Manim Compiler
 ↓
Timed Manim Code
 ↓
Render Attempt
 ↓
FFmpeg Merge
 ↓
Final Video

==================================================
REMOVE OPENAI
==================================================

Completely remove:
- OpenAI
- OpenAI TTS
- Claude dependencies

Replace with:
- Gemini
- NVIDIA NIM
- Piper

==================================================
API SETUP
==================================================

Use these environment variables:

GEMINI_API_KEY=
NVIDIA_API_KEY=

Optional:
ELEVENLABS_API_KEY=

==================================================
MODELS
==================================================

Use:

Main reasoning:
gemini-2.5-flash

Visual planning:
google/gemma-3-27b-it

Code generation:
deepseek-ai/deepseek-r1

==================================================
DEPENDENCIES
==================================================

Create a requirements.txt with:

manim
google-generativeai
openai
requests
python-dotenv
pydub
ffmpeg-python
whisperx
piper-tts
onnxruntime
moviepy

==================================================
INSTALLATION FIXES
==================================================

Fix all MacOS installation issues.

Detect:
- Apple Silicon vs Intel
- broken onnxruntime
- missing ffmpeg
- missing latex
- missing manim

Automatically generate:
setup.sh

that installs:

brew install ffmpeg
brew install basictex

and fixes PATH issues.

==================================================
DIRECTORY STRUCTURE
==================================================

Refactor into:

project/
│
├── main.py
├── .env
├── requirements.txt
│
├── modules/
│   ├── llm/
│   │   ├── gemini_client.py
│   │   ├── nvidia_client.py
│   │
│   ├── planning/
│   │   ├── scene_json.py
│   │   ├── narration.py
│   │   ├── visual_skeleton.py
│   │
│   ├── tts/
│   │   ├── piper_tts.py
│   │
│   ├── sync/
│   │   ├── whisper_align.py
│   │   ├── timeline_builder.py
│   │   ├── sync_engine.py
│   │
│   ├── manim/
│   │   ├── compiler.py
│   │   ├── renderer.py
│   │
│   ├── video/
│   │   ├── ffmpeg_merge.py
│
├── data/
│   ├── json/
│   ├── audio/
│   ├── manim/
│   ├── renders/
│   ├── timelines/

==================================================
IMPORTANT IMPLEMENTATION RULES
==================================================

1. TIMING MUST NEVER BE GENERATED BY LLMs

LLMs generate:
- narration text
- visual structure

ONLY.

Timing comes ONLY from:
- Piper audio duration
- WhisperX timestamps

2. Visual skeleton must NOT contain run_time.

3. Synchronization engine injects timing later.

4. Manim compiler converts:
visual skeleton + timeline
into final Manim code.

==================================================
SCENE JSON FORMAT
==================================================

Use this format:

{
  "scene_id": 1,
  "concept": "inertia",
  "anchor_example": "hockey puck",
  "narration": "...",
  "visual_instruction": "...",
  "beats": [
    {
      "phrase": "...",
      "visual": "..."
    }
  ]
}

==================================================
VISUAL SKELETON FORMAT
==================================================

Use:

{
  "scene_id": 1,
  "objects": [
    {
      "id": "title",
      "type": "Text",
      "content": "Newton's First Law",
      "action": "Write",
      "weight": 0.3
    }
  ]
}

==================================================
SYNC ENGINE
==================================================

Implement:

- duration extraction
- beat grouping
- timeline injection
- animation weight distribution

==================================================
MANIM COMPILER
==================================================

Generate final code like:

self.play(
    Write(title),
    run_time=timeline["title"]
)

==================================================
RENDER RETRY LOOP
==================================================

If Manim render fails:

1. Capture error
2. Send error + script to Gemini
3. Repair script
4. Retry max 3 times

==================================================
OUTPUT
==================================================

Final output:

data/renders/final_video.mp4

==================================================
ADDITIONAL REQUIREMENTS
==================================================

1. Add extensive logging
2. Add type hints
3. Add docstrings
4. Add clean modular imports
5. Ensure all paths are relative
6. Make code runnable immediately
7. Add fallback if WhisperX unavailable
8. Add fallback if Piper unavailable
9. Add sample prompts
10. Add sample Scene JSON
11. Add sample visual skeleton
12. Add sample synchronized timeline

==================================================
CRITICAL
==================================================

The project MUST run locally with:

python main.py

WITHOUT requiring:
- OpenAI
- Claude
- paid APIs

Only:
- Gemini
- NVIDIA
- Piper
- WhisperX
- Manim

==================================================
FINAL TASK
==================================================

1. Refactor the entire project
2. Install dependencies
3. Fix all imports
4. Create missing files
5. Create setup.sh
6. Create .env.example
7. Ensure pipeline runs end-to-end
8. Generate a demo video for:
   "Explain Force"

Do everything automatically.